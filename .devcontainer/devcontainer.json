{
  "name": "Intel-AI-PC", // Give a descriptive name
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu", // Or another suitable base image
  "features": {
    "ghcr.io/rocker-org/devcontainer-features/miniforge:2": {}
  },

  // Install additional packages
  "customizations": {
    "packages": [
      // Example:
      "git",
      "curl",
      "wget",
      "python3-pip",
      "apt",
      "htop",
      "nano"
    ]
  },

  // Post-create command (runs after the container is created)
  "postCreateCommand": "conda init bash && conda activate base && conda install -c conda-forge -y mamba && mamba install -c conda-forge --file requirements.txt && pip3 install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/xpu && conda clean --all"
}
